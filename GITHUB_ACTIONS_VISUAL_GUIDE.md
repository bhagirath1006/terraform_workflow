# 📋 GitHub Actions Workflow - Visual Guide

## Deployment Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         Developer                              │
│                    (You on Your Machine)                       │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ git push
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                      GitHub Repository                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ main branch                                                │ │
│  │ ├── .github/workflows/terraform-deploy.yml ← WORKFLOW    │ │
│  │ ├── terraform.tfvars                                     │ │
│  │ ├── main.tf                                              │ │
│  │ └── modules/                                             │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ Triggers Workflow
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│              GitHub Actions - Terraform Deploy                 │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ JOB 1: PLAN                                             │  │
│  │ ├─ Checkout Code                                        │  │
│  │ ├─ Setup Terraform                                      │  │
│  │ ├─ Configure AWS Credentials (from Secrets)            │  │
│  │ ├─ Terraform Init                                       │  │
│  │ ├─ Terraform Validate                                   │  │
│  │ ├─ Terraform Format Check                               │  │
│  │ ├─ Terraform Plan                                       │  │
│  │ ├─ Upload Plan Artifact                                 │  │
│  │ └─ Comment on PR (if Pull Request)                      │  │
│  └─────────────────────────────────────────────────────────┘  │
│                           │                                    │
│                    Only on main branch                         │
│                           ↓                                    │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ JOB 2: APPLY                                            │  │
│  │ ├─ Checkout Code                                        │  │
│  │ ├─ Setup Terraform                                      │  │
│  │ ├─ Configure AWS Credentials (from Secrets)            │  │
│  │ ├─ Download Plan Artifact                               │  │
│  │ ├─ Terraform Init                                       │  │
│  │ ├─ Terraform Apply                                      │  │
│  │ ├─ Get Outputs (website_url, elastic_ip, etc)          │  │
│  │ ├─ Create Deployment Summary                            │  │
│  │ └─ Send Slack Notification (if configured)             │  │
│  └─────────────────────────────────────────────────────────┘  │
│                           │                                    │
└───────────────────────────┼────────────────────────────────────┘
                            │
                            │ Deploys Resources
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                        AWS Account                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ VPC (10.0.0.0/16)                                          │ │
│  │ ├─ Internet Gateway                                        │ │
│  │ ├─ Public Subnet (10.0.1.0/24)                            │ │
│  │ ├─ Security Groups                                         │ │
│  │ └─ Route Table                                             │ │
│  │                                                            │ │
│  │ EC2 Instance (t2.micro)                                    │ │
│  │ ├─ Ubuntu 22.04 LTS                                        │ │
│  │ ├─ Docker Installed                                        │ │
│  │ ├─ Nginx Container Running                                 │ │
│  │ └─ SSH Key Configured                                      │ │
│  │                                                            │ │
│  │ Elastic IP (XXX.XXX.XXX.XXX)                               │ │
│  │ └─ Associated with EC2 Instance                            │ │
│  │                                                            │ │
│  │ CloudWatch Monitoring                                      │ │
│  │ └─ Enabled                                                 │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ Access Application
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Your Website                                 │
│            http://XXX.XXX.XXX.XXX (Elastic IP)                 │
│                 ↓ Running Nginx Container                       │
│         Welcome to Nginx! (or your app)                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## Pull Request Flow

```
┌────────────────────────────────────────────────────────┐
│ Developer Creates Feature Branch                       │
│ git checkout -b feature/update-docker                 │
└───────────────────┬────────────────────────────────────┘
                    │
                    │ Make changes to terraform.tfvars
                    │ or .tf files
                    │
                    ↓
┌────────────────────────────────────────────────────────┐
│ Push Branch and Create PR                              │
│ git push origin feature/update-docker                 │
│ (Create Pull Request on GitHub)                       │
└───────────────────┬────────────────────────────────────┘
                    │
                    │ GitHub detects PR
                    ↓
┌────────────────────────────────────────────────────────┐
│ GitHub Actions - PLAN JOB RUNS                         │
│ ✓ Validates Terraform                                 │
│ ✓ Creates Plan                                        │
│ ✓ Comments Plan on PR                                 │
└───────────────────┬────────────────────────────────────┘
                    │
                    ↓
        ┌───────────────────────┐
        │ Review Plan in PR      │
        │ Check what will change│
        └───────┬───────────────┘
                │
                ├─ Looks Good? → Approve & Merge
                │
                └─ Problems? → Request Changes
                            → Push fixes
                            → Rerun plan
                                ↓
        ┌───────────────────────────┐
        │ Another Plan Runs          │
        │ With Updated Code          │
        └───────────────────────────┘
                │
                ↓
    ┌──────────────────────────┐
    │ Merge to Main Branch      │
    │ (GitHub "Merge PR" button)│
    └────────┬─────────────────┘
             │
             │ GitHub detects merge to main
             ↓
    ┌──────────────────────────┐
    │ GitHub Actions - APPLY   │
    │ Downloads latest plan    │
    │ Applies to AWS           │
    │ Resources created! ✓     │
    │ Outputs generated        │
    └──────────────────────────┘
```

---

## Secrets Configuration

```
┌─────────────────────────────────────────────────────────┐
│ Your Local Machine (DO NOT COMMIT)                      │
│ ~/.ssh/docker-web-app-key          (Private Key)       │
│ ~/.ssh/docker-web-app-key.pub      (Public Key)        │
│ ~/.aws/credentials                 (AWS Config)        │
│ $env:VAULT_TOKEN                   (Vault Token)       │
└──────────────────┬──────────────────────────────────────┘
                   │
      Copy values (NOT files)
                   │
                   ↓
┌──────────────────────────────────────────────────────┐
│ GitHub Repository Secrets                            │
│ Settings → Secrets and variables → Actions           │
│                                                      │
│ AWS_ACCESS_KEY_ID                                    │
│ AWS_SECRET_ACCESS_KEY                                │
│ VAULT_TOKEN                                          │
│ SSH_PUBLIC_KEY_PATH                                  │
└──────────────────┬───────────────────────────────────┘
                   │
        Referenced in Workflow
                   │
                   ↓
┌──────────────────────────────────────────────────────┐
│ GitHub Actions - Runtime Environment                │
│ ${{ secrets.AWS_ACCESS_KEY_ID }}                     │
│ ${{ secrets.AWS_SECRET_ACCESS_KEY }}                 │
│ ${{ secrets.VAULT_TOKEN }}                           │
│ ${{ secrets.SSH_PUBLIC_KEY_PATH }}                   │
│                                                      │
│ Used to authenticate and deploy                     │
└──────────────────────────────────────────────────────┘
```

---

## Monitoring Status

```
GitHub → Actions Tab → Terraform Deploy

┌─────────────────────────────────────────────────────┐
│ Workflow Run History                                │
├─────────────────────────────────────────────────────┤
│ ✓ PASSED  Deploy infrastructure - main             │
│           Ran 2 minutes ago                         │
│                                                    │
│ ✓ PASSED  Test PR validation - feature/test       │
│           Ran 5 minutes ago                         │
│                                                    │
│ ✗ FAILED  Connection error - main                 │
│           Ran 10 minutes ago                        │
└─────────────────────────────────────────────────────┘

Click on any run to see:
├─ Job Summary
├─ Terraform Plan output
├─ Terraform Apply output
├─ Deployment Summary
│  ├─ Website URL
│  ├─ Elastic IP
│  ├─ Instance ID
│  └─ Docker status
└─ Logs (for troubleshooting)
```

---

## Environment Variables & Secrets Flow

```
┌────────────────────────────────┐
│ terraform.tfvars               │
│ (Your Configuration)           │
│                                │
│ aws_region = "us-east-1"       │
│ instance_type = "t2.micro"     │
│ docker_image = "nginx:latest"  │
└────────────┬───────────────────┘
             │
             │ Pushed to GitHub
             ↓
┌────────────────────────────────┐
│ GitHub Repository              │
│ (Tracked by Git)               │
└────────────┬───────────────────┘
             │
             │ Workflow reads:
             │ terraform.tfvars (public)
             │ GitHub Secrets (encrypted)
             ↓
┌─────────────────────────────────────────────┐
│ GitHub Actions Workflow Execution           │
│                                             │
│ export TF_VAR_aws_region="us-east-1"       │
│ export AWS_ACCESS_KEY_ID="AKIAxxxx"        │
│ export AWS_SECRET_ACCESS_KEY="wJalrxxxx"   │
│ export VAULT_TOKEN="root"                  │
│                                             │
│ terraform plan                              │
│ terraform apply                             │
└─────────────────────────────────────────────┘
             │
             │ Authenticates with AWS
             ↓
┌────────────────────────────────┐
│ AWS Account                    │
│ Creates Resources              │
│ VPC, EC2, EIP, SG, etc.        │
└────────────────────────────────┘
```

---

## Typical Timeline

```
10:00 - Start work
  └─ Create feature branch: git checkout -b update-image
  └─ Edit terraform.tfvars
  └─ Commit: git commit -m "Update Docker image"
  └─ Push: git push origin update-image

10:05 - Create PR on GitHub
  └─ GitHub automatically triggers Plan job
  └─ Workflow runs (2-3 minutes)
  └─ Results comment on PR

10:08 - Review plan
  └─ Check what will change
  └─ Looks good? Click "Merge pull request"

10:10 - Merge triggers Apply
  └─ Workflow runs Apply (2-3 minutes)
  └─ Resources created/updated
  └─ Deployment summary generated

10:13 - Check results
  └─ View Actions tab
  └─ See deployment summary
  └─ Access website with new settings

10:15 - Done!
  └─ Infrastructure updated
  └─ No manual steps needed
  └─ Changes tracked in Git history
```

---

## Key Files

### 1. Workflow Definition
```
.github/workflows/terraform-deploy.yml
├─ Triggers (push, PR, manual)
├─ Jobs (Plan, Apply, Cleanup)
├─ Steps (checkout, terraform, notify)
└─ Secrets configuration
```

### 2. Configuration
```
terraform.tfvars
├─ AWS settings
├─ Docker settings
├─ Vault settings
└─ SSH configuration
```

### 3. Infrastructure Code
```
main.tf, variables.tf, outputs.tf
├─ Module orchestration
├─ Variable definitions
└─ Output definitions

modules/
├─ vpc/main.tf
├─ ec2/main.tf
├─ eip/main.tf
├─ docker/main.tf
└─ vault/main.tf
```

---

## Success Indicators

✅ Workflow shows **green checkmarks** in Actions tab  
✅ Plan includes your expected changes  
✅ Apply completes without errors  
✅ Deployment summary shows outputs  
✅ Website accessible at elastic IP  
✅ Docker container running  
✅ Git history shows commits  

---

This visual guide helps you understand the complete GitHub Actions workflow for your Terraform infrastructure!
